article.flex.flex-col.w-full(class="smDown:flex-col smDown:items-start")
  div
    img.w-16.h-16.rounded-xl.object-cover(
      class="float-left mr-4 border border-l6"
      src="{image}"
      alt="{name}"
    )
    div
      span(class="text-h4-s xs-up:text-h5-s flex flex-wrap flex-col xs-up:flex-row")
        +if('link && link.length > 0')
          a(
            href="{link}"
            class="text-h4-s xs:text-h5-s font-satoshi"
            target="_blank"
            rel="noopener noreferrer"
          )
            | {name}
          +else
            span(class="text-h4-s xs:text-h5-s font-satoshi")
              | {name}
        span(class="text-h4-s text-t2 xs:text-h5-s font-satoshi")
          | &#160;—&#160; {role}
      p(class="mt-2 xs:mt-0 text-t2 xs:text-paragraph-s")
        | {description}
