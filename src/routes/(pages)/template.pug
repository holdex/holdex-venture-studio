include ./common-mixins

+main-section
    +header-wrapper()()
        h1(class="text-title-l font-satoshi text-center smDown:text-left xs:text-h1-s" id="{message.id}")
            | At #[TextHighlight Holdex], we're helping innovators build the next web3 products
    +content-wrapper
        +await('handleChartData()')
            Chart
            +then('chartData')
                Chart(info="{chartData.info}" prices="{chartData.prices}")
        BodyRenderer(blocks="{message.blocks}")
        p(class="text-t1") {@html signature}