include ../common-mixins
include ../c/mixins
+main-section
  +header-wrapper
    +nav-crumbs
      +crumbItemLink("/", "Holdex")
      +crumbDelimiter
      +crumbItemText("About")
    h1.text-title-l.font-satoshi.text-center(class="smDown:text-left xs:text-title-s" id="{message.id}") About Holdex
  +content-wrapper
    BodyRenderer(blocks="{message.blocks}")
